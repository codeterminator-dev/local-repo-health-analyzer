[project]
name = "local-repo-health-analyzer"
version = "0.1.0"
description = "Analyze local repository dependency topology without mutating source code."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

