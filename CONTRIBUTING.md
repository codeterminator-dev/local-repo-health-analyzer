# Contributing

感谢你为 `local-repo-health-analyzer` 做贡献。当前仓库仍在初始化阶段，请优先遵守本文档中的协作约定，保证多容器、多角色并行开发时具备可预测性。

## 基本原则

- 所有开发都基于远程仓库进行，禁止使用本地 `file://` 路径协作
- 默认从最新 `main` 创建分支，不直接提交到 `main`
- 变更应保持小而聚焦，并附带可复现的验证说明
- 不在未达成共识的情况下引入破坏性重构或无关格式化

## 分支命名规范

请使用以下前缀：

- `feature/*`: 新增功能、模块或分析能力
- `bugfix/*`: 修复缺陷、稳定性问题或回归
- `infrastructure/*`: 仓库治理、CI、文档、脚手架与工程配置

示例：

- `feature/report-summary`
- `bugfix/path-parser`
- `infrastructure/setup`

## 开发流程

1. 同步远程默认分支
2. 基于 `main` 创建主题分支
3. 完成修改后进行自检
4. 推送到远程并创建 Pull Request
5. 至少等待一次代码评审后再合并到 `main`

推荐命令：

```bash
git checkout main
git pull origin main
git checkout -b feature/your-topic
```

## Pull Request 要求

每个 Pull Request 至少应包含以下内容：

- 变更目的：本次修改解决什么问题
- 主要改动：新增或调整了哪些文件/行为
- 验证方式：如何在新容器或干净环境中复现验证
- 风险说明：是否存在未覆盖场景、后续工作或依赖项

PR 合并前请确认：

- 分支命名符合规范
- 描述完整且便于 reviewer 理解
- 涉及行为变化时，已经补充必要测试或明确说明为何暂不补充
- 不包含无关文件和临时调试内容

## 代码规范

当前项目尚未固定技术栈，提交代码时请遵守以下通用约束：

- 优先选择可读性和可维护性，而不是过早优化
- 新增模块应职责单一，避免隐式副作用
- 命名应准确反映用途，避免缩写堆叠
- 文档、脚本和配置变更应与实现保持同步
- 如果暂未补测试，应在 PR 中明确说明原因与后续补齐计划

## Reviewer 关注点

reviewer 会优先检查以下方面：

- 是否偏离 PR 声明的目标
- 是否引入明显的行为回归或协作风险
- 是否缺少关键验证步骤
- 是否违反分支、提交或文档规范

## 沟通建议

- 开始开发前，在 PR 或任务上下文中说明认领范围
- 如果发现仓库约束不足，优先补文档或模板，再推进功能实现
- 如需更改协作规则，请单独提交 `infrastructure/*` 分支处理
